function alerta() {
    alert("boton presionado");
}

/*
axios.get('http://localhost:8080/api/docs/all')
    .then(function (response) {
        // Manejar la respuesta
        const documentos = response.data;
        console.log('Documentos de Identidad:');
        documentos.forEach(function (documento) {
            var data = documento.id + ' ' + documento.nameDocument;
            console.log(data);
        });
    })
    .catch(function (error) {
        // Manejar errores
        console.error('Error al obtener los documentos de identidad:', error);
    });

 */
